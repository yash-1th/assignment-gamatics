name: gamatics_india
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  firebase_core: ^1.0.3
  firebase_storage: ^8.0.3
  cloud_firestore: ^2.1.0
  url_launcher: ^6.0.3
  file_picker: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true